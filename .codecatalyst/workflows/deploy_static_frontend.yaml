Name: DeployFrontendToS3
SchemaVersion: "1.0"

Triggers:
  - Type: Push
    Branches:
      - main

Actions:
  DeployFrontend:
    Identifier: aws/build@v1
    Inputs:
      Sources:
        - WorkflowSource
    Compute:
      Type: EC2
    Environment:
      Name: env-deploy-frontend
    Configuration:
      Steps:
        - Run: echo "🚀 Deploying frontend ke S3..."
        - Run: aws s3 cp frontend/ s3://nita-frontend-bucket/ --recursive
